<project>
    <!--
       mvn compile war:inplace tomcat:inplace : builds and deploys an in-place class reloading war
       mvn tomcat:redeploy : reload the war (necessary for certain config and XML changes)
       mvn package : Builds war with dependencies in target/
       mvn -Dmaven.test.skip=true package : Same as above but no unit tests
       mvn site : Generate site documentation include javadoc and clover reports
    -->
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>gs</groupId>
        <artifactId>gs-meta</artifactId>
        <version>1</version>
    </parent>
    <artifactId>gs-web</artifactId>
    <name>GS Web Project</name>
    <version>${gs.version}</version>
    <packaging>war</packaging>

    <repositories>
        <!-- These get called in sequential order -->
        <repository>
            <id>gs-repo</id>
            <url>http://maven2.dev.greatschools.net</url>
        </repository>
        <repository>
            <id>central</id>
            <url>http://repo1.maven.org/maven2</url>
        </repository>
        <repository>
            <id>google-maven-gwt-plugin-repo</id>
            <url>http://gwt-maven.googlecode.com/svn/trunk/mavenrepo</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>google-maven-gwt-plugin-repo</id>
            <url>http://gwt-maven.googlecode.com/svn/trunk/mavenrepo</url>
        </pluginRepository>
    </pluginRepositories>

    <build>
        <directory>target</directory>
        <outputDirectory>src/webapp/WEB-INF/classes</outputDirectory>
        <finalName>${artifactId}-${version}</finalName>
        <testOutputDirectory>target/test-classes</testOutputDirectory>
        <sourceDirectory>src/java</sourceDirectory>
        <testSourceDirectory>src/test</testSourceDirectory>

        <resources>
            <resource>
                <directory>src/java</directory>
                <includes>
                    <include>**/*.xml</include>
                    <include>**/*.txt</include>
                </includes>
            </resource>
            <resource>
                <directory>src/conf</directory>
                <includes>
                    <include>*.properties</include>
                    <include>*.xml</include>
                </includes>
                <!-- filtering is on for context.xml -->
                <filtering>true</filtering>
            </resource>
            <resource>
                <directory>${basedir}/../GSData/src/conf</directory>
                <includes>
                    <include>log4j-production.properties</include>
                </includes>
            </resource>
        </resources>

        <!-- Plugins -->
        <plugins>
            <!--<plugin>-->
            <!--<artifactId>maven-idea-plugin</artifactId>-->
            <!--<configuration>-->
            <!--<jdkLevel>1.5</jdkLevel>-->
            <!--<deploymentDescriptorFile>src/webapp/WEB-INF/web.xml</deploymentDescriptorFile>-->
            <!--</configuration>-->
            <!--</plugin>-->

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <configuration>
                    <warSourceDirectory>${basedir}/src/webapp</warSourceDirectory>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>tomcat-maven-plugin</artifactId>
                <configuration>
                    <update>true</update>
                    <path>/</path>
                    <mode>context</mode>
                    <contextFile>src/webapp/WEB-INF/classes/context.xml</contextFile>
                </configuration>
            </plugin>

            <plugin>
                <artifactId>maven-clean-plugin</artifactId>
                <configuration>
                    <filesets>
                        <fileset>
                            <directory>src/webapp/WEB-INF/lib</directory>
                        </fileset>
                        <fileset>
                            <directory>src/webapp/WEB-INF/classes</directory>
                        </fileset>
                        <fileset>
                            <directory>src/webapp/WEB-INF/gwtBin</directory>
                        </fileset>
                        <fileset>
                            <directory>src/webapp/res/gwt</directory>
                        </fileset>
                        <fileset>
                            <directory>src/webapp/META-INF</directory>
                        </fileset>
                        <fileset>
                            <directory>.gwt-cache</directory>
                        </fileset>
                    </filesets>
                </configuration>
            </plugin>

            <plugin>
                <groupId>com.totsp.gwt</groupId>
                <artifactId>maven-googlewebtoolkit2-plugin</artifactId>
                <version>2.0-beta15</version>
                <configuration>
                    <logLevel>ERROR</logLevel>
                    <gwtVersion>1.4.60</gwtVersion>
                    <compileTargets>
                        <compileTarget>gs.web.admin.gwt.TableCopyGWTPanel</compileTarget>
                    </compileTargets>
                    <runTarget>gs.web.admin.gwt.TableCopyGWTPanel/TableCopy.html</runTarget>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <!-- These two goals enable automatic GWT setup, but are expirimental -->
                            <goal>setup</goal>
                            <goal>extractGwt</goal>
                            <!--<goal>generateClientBeans</goal>-->
                            <!--<goal>mergewebxml</goal>-->
                            <goal>compile</goal>
                            <!--<goal>testGwt</goal>-->
                            <!--<goal>gwt</goal>-->
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Clover code coverage plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clover-plugin</artifactId>
                <configuration>
                    <licenseLocation>${basedir}/../GSData/metadata/maven/clover.license</licenseLocation>
                    <jdk>1.5</jdk>
                </configuration>
                <executions>
                    <execution>
                        <phase>pre-site</phase>
                        <goals>
                            <goal>instrument</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <!-- Generate the build.timestamp file -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <id>build timestamp</id>
                        <phase>process-resources</phase>
                        <configuration>
                            <tasks>
                                <echo>Creating build.timestamp property file</echo>
                                <propertyfile file="src/webapp/WEB-INF/classes/version.properties">
                                    <entry key="gsweb.buildtime"
                                           type="date"
                                           default="now"
                                           pattern="yyyyMMddkkmmss"/>
                                </propertyfile>
                            </tasks>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>relocate compiled gwt files</id>
                        <phase>process-classes</phase>
                        <configuration>
                            <tasks>
                                <mkdir dir="src/webapp/res/gwt"/>
                                <move todir="src/webapp/res/gwt">
                                    <fileset dir="target/${artifactId}-${version}/gs.web.admin.gwt.TableCopyGWTPanel"
                                             includes="**/*"/>
                                </move>
                                <delete dir="target/${artifactId}-${version}/gs.web.admin.gwt.TableCopyGWTPanel"/>
                            </tasks>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>ant</groupId>
                        <artifactId>ant-nodeps</artifactId>
                        <version>1.6.5</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>

    <reporting>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clover-plugin</artifactId>
                <configuration>
                    <licenseLocation>${basedir}/../GSData/metadata/maven/clover.license</licenseLocation>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>
        </plugins>
    </reporting>

    <!-- Search http://mvnrepository.com for new dependencies -->
    <dependencies>
        <dependency>
            <groupId>gs</groupId>
            <artifactId>gs-data</artifactId>
            <version>${gs.version}</version>
        </dependency>
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>servlet-api</artifactId>
            <version>2.4</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>jsp-api</artifactId>
            <version>2.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>jstl</artifactId>
            <version>1.1.2</version>
        </dependency>
        <dependency>
            <groupId>taglibs</groupId>
            <artifactId>standard</artifactId>
            <version>1.1.2</version>
        </dependency>
        <dependency>
            <groupId>taglibs</groupId>
            <artifactId>taglibs-cache</artifactId>
            <version>1.2</version>
        </dependency>
        <dependency>
            <groupId>opensymphony</groupId>
            <artifactId>sitemesh</artifactId>
            <version>2.2.1</version>
        </dependency>
        <dependency>
            <groupId>displaytag</groupId>
            <artifactId>displaytag</artifactId>
            <version>1.1</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <version>2.5.3</version>
        </dependency>
        <dependency>
            <groupId>jdom</groupId>
            <artifactId>jdom</artifactId>
            <version>1.0</version>
        </dependency>
        <dependency>
            <groupId>rome</groupId>
            <artifactId>rome</artifactId>
            <version>0.8</version>
        </dependency>
        <dependency>
            <groupId>com.google.gwt</groupId>
            <artifactId>gwt-servlet</artifactId>
            <version>1.4.60</version>
        </dependency>
        <dependency>
            <groupId>com.google.gwt</groupId>
            <artifactId>gwt-user</artifactId>
            <version>1.4.60</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jfree</groupId>
            <artifactId>jfreechart</artifactId>
            <version>1.0.5</version>
        </dependency>
        <dependency>
            <groupId>jfree</groupId>
            <artifactId>jcommon</artifactId>
            <version>1.0.9</version>
        </dependency>
        <dependency>
            <groupId>jsptags</groupId>
            <artifactId>pager-taglib</artifactId>
            <version>2.0</version>
        </dependency>
        <dependency>
            <groupId>gdata</groupId>
            <artifactId>gdata-core</artifactId>
            <version>1.0</version>
        </dependency>
        <dependency>
            <groupId>gdata</groupId>
            <artifactId>gdata-client</artifactId>
            <version>1.0</version>
        </dependency>
        <dependency>
            <groupId>gdata</groupId>
            <artifactId>gdata-spreadsheet</artifactId>
            <version>1.0</version>
        </dependency>
        <dependency>
            <groupId>recaptcha4j</groupId>
            <artifactId>recaptcha4j</artifactId>
            <version>0.0.7</version>
        </dependency>
        <!-- Test only dependencies -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <version>2.5.3</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>httpunit</groupId>
            <artifactId>httpunit</artifactId>
            <version>1.6</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>