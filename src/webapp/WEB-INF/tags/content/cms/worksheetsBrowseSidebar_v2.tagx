<?xml version="1.0" encoding="UTF-8"?>
<jsp:root version="2.0"
          xmlns:jsp="http://java.sun.com/JSP/Page"
          xmlns:c="http://java.sun.com/jsp/jstl/core">

    <c:if test="${not empty worksheetsLinks}">

        <div class="hd">
            <h6>Worksheets</h6>
        </div>
        <div class="bd clearfix">
            <ul>
                <c:forEach var="grade" items="${worksheetsLinks}">
                    <c:forEach var="gradeMap" items="${grade}">
                        <li class="hasNested">
                            <div class="mediaExt1-1">
                                <span class="sprite twirlyClosed img"><!-- do not collapse --></span>
                                <a class="outerNavLink bk" href="javascript:void(0);">${gradeMap.key.linkText}</a>
                            </div>
                            <ul class="nested">
                                <c:forEach var="subject" items="${gradeMap.value}">
                                    <li><a href="${subject.url}">${subject.linkText}</a></li>
                                </c:forEach>
                                <li class="lastOne"><a href="${gradeMap.key.url}">See all &amp;raquo;</a></li>
                            </ul>
                        </li>
                    </c:forEach>
                </c:forEach>
            </ul>
        </div>

    </c:if>
</jsp:root>