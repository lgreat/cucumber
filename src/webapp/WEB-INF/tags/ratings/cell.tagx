<?xml version="1.0" encoding="UTF-8"?>
<jsp:root version="2.0"
          xmlns:jsp="http://java.sun.com/JSP/Page"
          xmlns:c="http://java.sun.com/jsp/jstl/core"
          xmlns:gsml="urn:jsptagdir:/WEB-INF/tags/gsml"
          xmlns:promo="urn:jsptagdir:/WEB-INF/tags/promo"
          xmlns:ad="urn:jsptagdir:/WEB-INF/tags/ad"
          xmlns:link="urn:www.greatschools.net/taglib/link"
          xmlns:pageHelper="urn:jsptagdir:/WEB-INF/tags/pageHelper"
        >
    <jsp:directive.tag body-content="scriptless"/>
    <jsp:directive.attribute name="cell" required="true" description="Object that has a rating and trend"
                             type="gs.web.test.rating.Cell"/>
    <c:choose>
        <c:when test="${not empty cell.rating}">
            <c:choose>
                <c:when test="${not empty cell.trend}">
                    <c:set var="trend" value="${cell.trend}"/>
                    <c:choose>
                        <c:when test="${trend ge 2}">
                            ${cell.rating} <gsml:img src="/images/carrowts2.gif" alt="Strong improvement"/>
                        </c:when>
                        <c:when test="${trend == 1}">
                            ${cell.rating} <gsml:img src="/images/carrowts1.gif" alt="Some improvement"/>
                        </c:when>
                        <c:when test="${trend == 0}">
                            ${cell.rating} <gsml:img src="/images/carrowts1right.gif" alt="No change"/>
                        </c:when>
                        <c:when test="${trend == -1}">
                            ${cell.rating} <gsml:img src="/images/carrowts1dn.gif" alt="Some decline"/>
                        </c:when>
                        <c:when test="${trend le -2}">
                            ${cell.rating} <gsml:img src="/images/carrowts2dn.gif" alt="Strong decline"/>
                        </c:when>
                        <c:otherwise>${cell.rating}</c:otherwise>
                    </c:choose>
                </c:when>
                <c:otherwise>${cell.rating}</c:otherwise></c:choose>
        </c:when>
        <c:otherwise>n/a</c:otherwise>
    </c:choose>
</jsp:root>
